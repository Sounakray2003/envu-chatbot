# Core Framework
pydantic==2.12.5
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.20
# Unused in current codebase:
# pydantic-settings==2.6.0
python-dotenv==1.0.1
huggingface-hub==0.34.4
pillow==11.0.0
# Unused in current codebase:
# accelerate==1.10.1
pytesseract==0.3.13
beautifulsoup4==4.12.3

# LLM & Embeddings
# Unused in current codebase:
# langchain-core==1.2.11
# Optional higher-level LangChain package:
# langchain==1.2.6
# anthropic==0.39.0
# Unused in current codebase:
# sentence-transformers==5.2.2
# torch==2.9.1
transformers>=4.57.0

# Vector Storage
# Qdrant access uses direct HTTP requests in the current codebase.

# Document Processing
# pdfplumber==0.11.4
# Unused in current codebase:
# pypdf==6.7.0
python-docx==1.1.2
# Unused in current codebase:
# python-pptx==1.0.2
openpyxl==3.1.5
pandas==2.2.3

# Text & NLP
nltk==3.9.1
tiktoken==0.12.0

# SSL & Certificates
# certifi is installed transitively by requests/httpx; no direct import here.

# HTTP & Networking
requests==2.32.3
httpx==0.27.2
trafilatura==2.0.0
# Unused in current codebase:
# tenacity==9.1.2

# Legacy Kafka dependency retained only if external tooling still needs it:
# confluent-kafka==2.7.0

# Utilities
# Unused in current codebase:
# colorama==0.4.6
# tqdm==4.67.1

# PDF extraction
imagehash
docling
unstructured[pdf]
pymupdf
